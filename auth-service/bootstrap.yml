spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://127.0.0.1:8888
      fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

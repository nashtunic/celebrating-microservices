

import 'package:flutter/material.dart';

class Constants {
  static const primaryColor = Colors.orange; // Blue color for primary theme

  static String formatDate(DateTime date) {
    return '${date.day}-${date.month}-${date.year}';
  }
}




